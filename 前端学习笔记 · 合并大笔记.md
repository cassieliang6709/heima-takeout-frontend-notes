---
title: 黑马外卖 · 前端学习笔记（合并版）
course: 黑马外卖前端课程
created: 2026-09-14
updated: 2026-09-15
status: 学习中
tags:
  - 前端
  - 黑马外卖
---

# 黑马外卖 · 前端学习笔记（合并版）

> 本笔记由 7 份小笔记合并而成（00 课程索引 ~ 06 GitHub 索引），并补充了「工程化配置」章节。
> 一句话串起来：**HTML 搭结构，CSS 管外观，JavaScript 写逻辑；Vue 组织复杂页面，axios 请求后端数据，Element UI 提供现成界面组件。**

## 目录

- [[#一、课程总览|一、课程总览]]
- [[#二、HTML 与 CSS|二、HTML 与 CSS]]
- [[#三、JavaScript|三、JavaScript]]
- [[#四、axios|四、axios]]
- [[#五、Vue 基础语法|五、Vue 基础语法]]
- [[#六、Element UI|六、Element UI]]
- [[#七、工程化配置（Vue CLI 项目结构与端口）|七、工程化配置（Vue CLI 项目结构与端口）]]
- [[#八、GitHub 代码与学习笔记索引|八、GitHub 代码与学习笔记索引]]
- [[#九、延伸参考（外部权威资料）|九、延伸参考（外部权威资料）]]

---

## 一、课程总览

### 五个知识点

| 知识点 | 是什么 | 黑马外卖里会做什么 | 工作中的使用情况 |
|---|---|---|---|
| HTML 与 CSS | HTML 定义页面结构和语义；CSS 控制颜色、尺寸、间距和布局 | 搭登录页、列表、表单，调整后台管理页面布局 | **前端必用**，全栈也要会 |
| JavaScript | 真正负责数据、判断、事件和异步操作的编程语言 | 点击按钮、表单校验、处理订单和菜品数据 | **前端核心**；Node.js 后端也用 |
| axios | 帮 JavaScript 发送 HTTP 请求的工具库 | 登录、获取菜品、修改订单状态、提交表单 | 很常见，但可被原生 `fetch` 等替代 |
| Vue 基础语法 | 基于 JavaScript 的组件化、响应式 UI 框架 | 数据变化时自动更新页面，拆分列表、弹窗、表单等组件 | Vue 技术栈岗位常用；React 岗位不用 Vue |
| Element UI | Vue 的现成 UI 组件库 | 快速做表格、分页、按钮、表单、弹窗和提示 | 后台管理系统常见，但不是前端通用基础 |

### 它们怎样协作

```text
用户点击 Element UI 按钮
        ↓
Vue 调用 JavaScript 方法
        ↓
axios 向后端 API 发请求
        ↓
返回订单 / 菜品数据
        ↓
Vue 更新状态，HTML + CSS 渲染新页面
```

### 工作中应该掌握到什么程度

- **一定要打牢：HTML、CSS、JavaScript。** 换成 Vue、React 或其他框架仍然要用。
- **Vue 要会项目级基础：**组件、响应式状态、事件、条件/列表渲染、表单绑定、路由和生命周期。
- **axios 不要只背写法：**重点理解 HTTP、GET/POST、请求参数、响应、状态码、`async/await` 和异常处理。
- **Element UI 会查文档即可：**不用背全部组件；工作中通常按设计稿查表格、表单、分页和弹窗的 API。
- **版本提醒：**老课程的 `Element UI` 通常配 Vue 2；Vue 3 项目通常使用 `Element Plus`。两者思路相近，但 API 和安装方式不能直接混用。

### 建议学习顺序

`HTML/CSS → JavaScript → HTTP 与异步 → Vue → axios → Element UI`

### 官方资料

- [MDN：HTML、CSS 与 JavaScript](https://developer.mozilla.org/en-US/docs/Learn_web_development/Getting_started/Web_standards/The_web_standards_model)
- [Vue 3 官方简介](https://vuejs.org/guide/introduction)
- [Axios 官方文档](https://axios-http.com/docs/intro)
- [Element UI（Vue 2）](https://element.eleme.io/) · [Element Plus（Vue 3）](https://element-plus.org/en-US/guide/installation)

---

## 二、HTML 与 CSS

### 先记住一句话

**HTML 负责"页面里有什么"，CSS 负责"它们长什么样、放在哪里"。**

例如，HTML 先放一个按钮：

```html
<button class="buy-button">立即下单</button>
```

CSS 再把它变成蓝色：

```css
.buy-button {
  background: blue;
  color: white;
}
```

### 1. 根据设计稿还原页面

"还原设计稿"就是：**照着图片，用 HTML 放内容，用 CSS 排位置和调样式。**

假设设计稿中有 3 张菜品卡片：

- **Flexbox**：像排队，让元素横着或竖着排成一队。
- **Grid**：像棋盘，让元素按行和列排列。
- **间距**：`gap: 16px` 表示卡片之间留 16 像素的空白。
- **响应式**：电脑屏幕上排 3 列，手机屏幕窄时自动变成 1 列。

```css
.dish-list {
  display: grid;                         /* 使用网格排列 */
  grid-template-columns: repeat(3, 1fr); /* 平均分成 3 列 */
  gap: 16px;                             /* 卡片之间留空 */
}

@media (max-width: 600px) {              /* 屏幕宽度不超过 600px 时 */
  .dish-list { grid-template-columns: 1fr; } /* 改成 1 列 */
}
```

### 2. 写表单、按钮、表格和导航

**语义化**就是"用对的标签做对的事"：

| 页面内容 | 应使用 |
| --- | --- |
| 点击后执行操作 | `<button>` |
| 点击后跳到新页面 | `<a>` |
| 输入手机号 | `<input>` |
| 页面导航 | `<nav>` |
| 成行成列的数据 | `<table>` |

**可访问**是指：不用鼠标的人、使用屏幕阅读器的人，也能正常使用页面。

```html
<label for="phone">手机号</label>
<input id="phone" type="tel">
<button type="submit">提交订单</button>
```

`label` 告诉用户输入框要填什么；`for="phone"` 和 `id="phone"` 把文字与输入框配成一对。

### 3. 用浏览器工具找问题

页面上的按钮位置不对时：

1. 在按钮上点右键 → **检查**。
2. 右侧 **Styles** 会显示它用到的 CSS。
3. 修改 `width`、`margin`、`padding` 等数值，立刻看效果。
4. 这里的修改只是临时试验；试对后，还要回到代码文件中修改。

常见原因：间距设错、宽高设错，或自己写的 CSS 被别的 CSS 覆盖。

### 4. HTML/CSS 在 Vue 中怎么用

一个 `.vue` 文件可以先理解成三个房间：

- `<template>`：放 HTML，决定页面上有什么。
- `<script>`：放 JavaScript，决定点击后做什么。
- `<style>`：放 CSS，决定页面长什么样。

```vue
<template>
  <button class="buy-button" @click="buy">立即下单</button>
</template>

<style>
.buy-button { background: blue; color: white; }
</style>
```

`@click="buy"` 的意思是：**用户点击按钮时，执行名为 `buy` 的功能。**

### 现在先学什么

1. 先会用 HTML 写文字、图片、输入框和按钮。
2. 再学 CSS 的颜色、字号、宽高和间距。
3. 之后再学 Flexbox 和 Grid。
4. 响应式、可访问性和样式覆盖先知道是什么，暂时不需要背。

---

## 三、JavaScript

### 概念

JavaScript 是前端真正执行逻辑的编程语言。HTML/CSS 主要负责展示，JavaScript 负责变量、计算、判断、循环、函数、事件和网络请求。

```js
const order = { status: '待接单', amount: 68 }

function canAccept(order) {
  return order.status === '待接单'
}
```

### 工作中怎么用

- 响应点击、输入和提交等用户操作。
- 处理接口返回的数据和业务规则。
- 操作数组、对象、日期、字符串及页面状态。
- 使用 `Promise`、`async/await` 处理异步请求。
- Vue 本身建立在 JavaScript 之上，框架不能代替语言基础。

### 优先掌握

变量与类型、函数、数组与对象、条件与循环、作用域、模块、DOM、事件、Promise、`async/await`、错误处理。

---

## 四、axios

### 概念

axios 是一个基于 Promise 的 **HTTP 客户端库**。前端用它向后端 API 请求或提交数据；它不是编程语言，也不是 Vue 的一部分。

```js
const response = await axios.get('/api/orders', {
  params: { status: '待接单' }
})

const orders = response.data
```

### 工作中怎么用

- `GET` 查询订单或菜品，`POST` 新建，`PUT/PATCH` 修改，`DELETE` 删除。
- 统一配置服务器地址、超时时间和登录令牌。
- 用拦截器处理鉴权、公共请求头和统一错误提示。
- 处理加载中、请求失败、超时及取消请求。

### 真正需要理解

重点不是背 `axios.get()`，而是 HTTP 方法、URL、查询参数、请求体、请求头、状态码、JSON、Promise 和异常处理。项目也可能使用浏览器原生 `fetch`。

### Q&A：为什么 GET 要传 headers / params，POST 却"不用"？

不是 GET"要"、POST"不用"，而是**两个方法的参数位置设计不同**：

```js
axios.get(url, config)            // GET：没有请求体，第二个参数直接就是 config
axios.post(url, data, config)     // POST：第二个参数位置被 data（请求体）占了，config 在第三个
```

```js
// GET：查询参数和请求头都写在第二个参数（config）里
axios.get('/admin/employee/page', {
  params: { name: '张三', page: 1, pageSize: 10 },  // 自动拼到 URL 的 ? 后面
  headers: { 'token': 'xxx' }
})

// POST：第二个参数是请求体 JSON；要传 headers 就放到第三个参数
axios.post('/admin/employee/login', {
  username: 'admin',
  password: '123456'
}, {
  headers: { 'token': 'xxx' }
})
```

**背后的语义**：GET 的参数按 HTTP 规范本来就该放在 URL 查询串里（GET 请求没有 body），所以 axios 让你写在 `config.params`，它自动拼成 `?name=xxx`；POST 的数据放在请求体里，所以单独占一个参数位置。

一句话记忆：**GET 的"第二格"是配置，POST 的"第二格"是数据。**

#### 三个容易混淆的点

- `headers` 不是 GET 专属，也不是必填项；只有接口要求鉴权、特定 `Content-Type` 或其他请求头时才传。
- POST 如果既要传请求体又要传请求配置，必须把配置放在第三个参数；否则 axios 会把第二个对象当成 body。
- POST 也可以传查询参数，例如 `axios.post(url, data, { params: { id: 1 } })`；`params` 和 `data` 分别对应 URL 查询串和请求体。

```js
// 不需要额外请求头时，GET / POST 都可以这样写
axios.get('/api/users')
axios.post('/api/users', { name: 'Cassie' })
```

---

## 五、Vue 基础语法

### 概念

Vue 是构建用户界面的 JavaScript 框架。它的核心是：**用组件拆页面，用响应式状态驱动页面更新。** 开发者主要描述"数据是什么、页面应该长什么样"，Vue 负责在数据变化后更新 DOM。

{% raw %}
```vue
<script setup>
import { ref } from 'vue'
const count = ref(0)
</script>

<template>
  <button @click="count++">订单数：&#123;&#123; count &#125;&#125;</button>
</template>
```
{% endraw %}

### 工作中怎么用

- 将页面拆成订单表格、搜索栏、编辑弹窗等可复用组件。
- 根据状态显示或隐藏内容，循环渲染订单列表。
- 处理表单双向绑定、父子组件通信和路由切换。
- 调接口后修改状态，让页面自动同步。

### 优先掌握

插值表达式、`v-bind`、`v-on`、`v-if`、`v-for`、`v-model`、组件、props/emit、响应式状态、生命周期、路由。先确认课程使用 Vue 2 还是 Vue 3。

### 文本插值（`&#123;&#123; &#125;&#125;`，Mustache 语法）

- **作用**：绑定 `data` 方法返回的对象属性，把数据显示到页面上。
- **用法**：`&#123;&#123; 属性名 &#125;&#125;`，大括号里可以写简单的 JavaScript **表达式**。

{% raw %}
```vue
<template>
    <div>
        <h1>&#123;&#123; name &#125;&#125;</h1>
        <h1>&#123;&#123; age > 60 ? '老年' : '青年' &#125;&#125;</h1>
    </div>
</template>
<script>
    export default {
        data() {
            return { name: '张三', age: 30 };
        }
    };
</script>
```
{% endraw %}

页面显示结果：第一行 `张三`，第二行 `青年`（因为 30 不大于 60，三元运算符取 `:` 后面的值）。

三个要点：

1. `data()` 是 Vue 2 的写法：把页面要用的数据以对象形式 `return` 出去，`&#123;&#123; &#125;&#125;` 里就能直接引用属性名。
2. `&#123;&#123; &#125;&#125;` 里**只能写表达式**（能算出值的式子，如三元运算、算术、字符串拼接），**不能写语句**（如 `if`、`for`、`var` 声明）。
3. 插值是最基础的绑定方式；属性绑定用 `v-bind`（简写 `:`），双向绑定用 `v-model`，列表循环用 `v-for`。

### 属性绑定（`v-bind`，简写 `:`）

- **作用**：为 HTML 标签的属性绑定 `data` 中返回的属性值（`&#123;&#123; &#125;&#125;` 只能绑定标签中间的内容，属性要用 `v-bind`）。
- **用法**：`v-bind:属性名="data里的键"`，简写为 `:属性名="键"`。

```vue
<template>
    <div>
        <div><input type="text" v-bind:value="name"></div>
        <div><input type="text" :value="age"></div>   <!-- 简写，与上一行等价 -->
        <div><img :src="src"></div>
    </div>
</template>
<script>
    export default {
        data() {
            return {
                name: '王五',
                age: 20,
                src: 'https://www.itcast.cn/2018czgw/images/logo2.png'
            };
        }
    };
</script>
```

三个要点：

1. `:` 开头是 `v-bind` 的简写，**两种写法完全等价**，实际开发几乎只用简写。
2. 绑定后数据是**响应式**的：`data` 里的值一变，页面属性自动更新（图片地址换图、输入框值变化）。
3. 与插值分工：`&#123;&#123; &#125;&#125;` 管标签**中间的文本**，`:` 管标签**属性里的值**，两者都只能是表达式。

### 双向绑定（`v-model`）

- **作用**：把**表单输入项**和 `data` 中的属性**双向绑定**——用户在输入框里改，数据自动变；代码里改数据，输入框自动变。任意一方改变都会同步给另一方。
- **用法**：`v-model="data里的键"`，主要用于 `input`、`select`、`textarea` 等表单元素。

```vue
<template>
    <div>
        <div>
            双向绑定：&#123;&#123; name &#125;&#125;
            <input type="text" v-model="name" />
            <input type="button" value="改变" @click="handleChange"/>
        </div>
    </div>
</template>
<script>
    export default {
        data() { return { name: '张三' } },
        methods: {
            handleChange() {
                this.name = '李四'
            }
        }
    };
</script>
```

两种操作都会让页面同步更新：

1. 用户在输入框里打字 → `name` 跟着变 → 上面 `&#123;&#123; name &#125;&#125;` 实时刷新；
2. 点"改变"按钮执行 `handleChange()` → 代码把 `this.name` 改成 `'李四'` → 输入框内容自动变。

要点对比（三种绑定一次分清）：

| 方式 | 方向 | 典型场景 |
|---|---|---|
| `&#123;&#123; &#125;&#125;` 插值 | 数据 → 页面（单向） | 展示文本 |
| `:`（v-bind） | 数据 → 页面（单向） | 绑定属性：src、value、href |
| `v-model` | 数据 ⇄ 页面（双向） | 表单输入：input、select |

> 补充：方法要写在 `methods: { }` 里，通过 `this.属性名` 访问 `data` 中的数据；`@click="方法名"` 是点击事件的绑定写法。

### 条件渲染（`v-if` / `v-else-if` / `v-else`）

- **作用**：根据**表达式的值**来动态渲染页面元素——条件成立就显示这个元素，不成立就不渲染它（不是隐藏，是压根不生成）。
- **用法**：`v-if="表达式"`、`v-else-if="表达式"`、`v-else`，三者按顺序判断，和 Java 里的 `if / else if / else` 一个思路。

```vue
<template>
    <div>
        <div v-if="sex == 1">男</div>
        <div v-else-if="sex == 2">女</div>
        <div v-else>未知</div>
    </div>
</template>
<script>
    export default {
        data() {
            return { sex: 1 };
        }
    };
</script>
```

页面只显示 `男`：因为 `sex == 1` 成立，后面两个分支直接跳过；如果把 `sex` 改成 `2` 或 `3`，页面就会只显示 `女` 或 `未知`。

三个要点：

1. `v-if` 后面跟的也是**表达式**（判断条件），和 `&#123;&#123; &#125;&#125;`、`:` 的要求一致。
2. 三个指令必须**连续使用**且写在**兄弟元素**上：中间不能插入别的元素，否则 `v-else` / `v-else-if` 会找不到它跟谁配对而报错。
3. 它管的是"**这个元素要不要出现在页面上**"，和插值、绑定是两类事情：绑定决定元素"显示什么内容"，条件渲染决定元素"有没有"。

> 对比记忆：绑定三件套管内容（`&#123;&#123; &#125;&#125;` 文本、`:属性`、`v-model` 表单）；`v-if` 系列管元素有无；列表循环则对应 `v-for`。

---

## 六、Element UI

### 概念

Element UI 是面向 Vue 的 UI 组件库，相当于一盒已经设计、实现好的界面零件。开发者可以直接组合按钮、表格、表单、分页、弹窗和消息提示，而不必全部从零写 HTML/CSS。

```vue
<el-button type="primary">新增菜品</el-button>
<el-table :data="orders">...</el-table>
```

### 工作中怎么用

- 快速开发后台管理系统和内部工具。
- 通过组件的属性、事件和插槽实现业务交互。
- 配合 Vue 状态和 axios 数据完成列表、表单及分页。
- 按设计规范统一颜色、尺寸、校验和交互反馈。

### 注意

- `Element UI` 主要对应 **Vue 2**；`Element Plus` 对应 **Vue 3**。
- 它提高开发速度，但不能代替 HTML、CSS、JavaScript 和 Vue 基础。
- 工作中不需要背全部 API，重点是会读组件文档、示例和报错。

---

## 七、工程化配置（Vue CLI 项目结构与端口）

> 本章节对应课程截图中的 **Vue CLI 项目结构图** 和 **vue.config.js 配置**，是图片里牵扯到的知识点。
> 2026-09-15 根据课程视频（第 172 集）补充了「开发环境准备」「创建工程的两种方式」「启动与停止」三节，并根据后续课程截图补充了「跨域问题与前端代理」。

### 0. 开发环境准备

![[黑马程序员 Vue CLI 环境与启动.svg]]

开发前端项目前要先装好三样东西：

| 环境 | 是什么 | 类比后端 Java 开发 |
|---|---|---|
| Node.js | 前端项目的运行环境 | JDK |
| NPM | JS 包管理工具：下载第三方依赖包、运行项目；安装 Node.js 时自带 | Maven |
| Vue CLI | Vue 官方脚手架，用来快速创建前端工程 | Spring Initializr |

- 安装 Vue CLI（全局）：`npm install -g @vue/cli`（`i` = install，`-g` = 全局安装）
- 查看版本：`node -v`、`npm -v`

本图对应的最小命令清单：

```bash
vue create 项目名称   # 命令行创建工程
vue ui                # 图形化创建与管理工程
npm run serve         # 启动开发服务器
# Ctrl + C             # 停止服务
```

前端项目中的重点文件：`main.js`（入口）、`vue.config.js`（Vue CLI 配置）、`package.json`（依赖与脚本）、`App.vue`（根组件）。

### 1. 创建前端工程：两种方式

**方式一：命令行 `vue create 项目名`**

1. 在一个**不含中文路径**的目录下打开 CMD（如 `E:\vue_project`）
2. 执行 `vue create vue-demo1`，回车后选择 Vue 版本（本课程选 **Vue 2**）
3. 等待依赖下载完成，目录下即出现新项目

**方式二：图形界面 `vue ui`（⭐ 课程后续统一用这种方式）**

1. 执行 `vue ui`，会自动打开一个网页（"Starting GUI"）
2. 选「Vue 项目管理器」→ 选好创建位置 → 点「+ 创建」→「在此创建新项目」
3. 填项目名称、包管理器选 **NPM**、按需勾选 Git → 下一步 → 选 **Vue 2** → 创建项目
4. 创建完成后，网页里还能直接看到项目依赖了哪些包、装了哪些插件，并能一键启动服务

两种方式创建出来的**工程结构完全一样**，区别只是方式二在网页里勾选选项，更直观。创建时选择 Vue 2 还是 Vue 3 在两种方式里都会出现。

### 2. Vue CLI 工程结构

![[attachments/黑马程序员 Vue CLI 项目结构.png]]

- `node_modules/`：项目依赖的 JS 包（`npm install` 装出来的，不要手动改）
- `public/`：公共静态文件，入口模板是 `index.html`
- `src/assets/`：图片、样式等静态资源
- `src/components/`：可复用公共组件
- `App.vue`：根组件（所有页面的"总入口"）
- `main.js`：应用启动入口
- `package.json`：依赖、脚本和项目信息
- `vue.config.js`：Vue CLI 配置

> 注意：这是 **Vue CLI 工程结构**；参考仓库 OUC-Take-Out 使用 Vue 2 `<script>` 直引方式，两者不要混为一套目录。

补充：`package.json` 里 `dependencies` 是项目运行和上线打包都需要的依赖，`devDependencies` 只在开发时使用，不会打进上线包。

### 3. 启动与停止前端服务

在 VS Code 中打开工程文件夹（File → Open Folder），按 `Ctrl + `` `（或点右上角终端按钮）打开终端：

- **启动**：`npm run serve` —— 注意 `serve` 不是固定写法，它来自 `package.json` 里 `scripts` 的定义，改成别的名字命令也要跟着改
- **停止**：终端里按 `Ctrl + C`，输入 `Y` 回车；停掉后浏览器再刷新就访问不到了
- 启动成功后按住 `Ctrl` 点击终端里的地址，即可在浏览器打开首页

### 4. vue.config.js：配置前端服务端口号

`vue.config.js` 是 Vue CLI 项目的配置文件，项目启动时会自动读取。

```js
const { defineConfig } = require('@vue/cli-service')
module.exports = defineConfig({
  transpileDependencies: true,   // 让第三方依赖包也做语法兼容转换（默认开着即可）
  devServer: {                   // ⭐ 开发服务器的配置
    port: 7070                   // ⭐ 把端口号改成 7070
  }
})
```

**关键就一句：`port: 7070`。**

- 不加配置时，`npm run serve` 默认启动在 `http://localhost:8080`
- 加上后，启动地址变为 `http://localhost:7070`

### 5. 为什么要改端口？——避免"门牌号撞车"

**端口号是"门牌号"，一台电脑上同一个门牌号只能给一个人用：**

| 程序 | 默认端口 | 冲突后果 |
|---|---|---|
| 前端 Vue 项目 | 8080 | 两个程序抢 8080，后启动的会报错 |
| 黑马外卖后端 Java / Tomcat | 8080 | `Port 8080 is already in use` |

黑马外卖是"前端 + 后端"两个项目同时跑，如果都用 8080 就会撞车。课程约定：**后端占 8080，前端让出位置改用 7070**（改成其他数字也行，只要两边不一样）。

**排查技巧：**看到 `EADDRINUSE`（端口被占用）报错，就知道是门牌号撞了——要么关掉占用的程序，要么改配置换端口。

### 6. 跨域问题（CORS）与前端代理

> 本节对应课程 demo 截图：页面跑在 `http://localhost:7070`，点击"发送请求"调用后端 `http://localhost:8080/admin/employee/login`，浏览器 Console 报错（2026-09-15 补充）。

**报错长这样：**

```text
Access to XMLHttpRequest at 'http://localhost:8080/admin/employee/login' from origin
'http://localhost:7070' has been blocked by CORS policy: Response to preflight request
doesn't pass access control check: No 'Access-Control-Allow-Origin' header is present
on the requested resource.
POST http://localhost:8080/admin/employee/login net::ERR_FAILED
```

**什么是跨域**：协议、域名、端口只要有**任何一个不同**，就是"不同源"。前端在 7070、后端在 8080 —— 端口不同，所以是跨域请求。

**为什么被浏览器拦截**：浏览器有"同源策略"安全机制。前端发跨域请求时，浏览器会先发出一个**预检请求**（preflight，OPTIONS 方法）问后端："你允许我这个源访问吗？"后端返回的响应头里没有 `Access-Control-Allow-Origin`，浏览器就把响应扣下不给页面，表现为 `ERR_FAILED`。**注意：请求其实到达过后端，只是响应被浏览器拦了。**

**两种解决思路：**

1. **后端放行（CORS 配置）**：后端在响应头里加上 `Access-Control-Allow-Origin`，明确告诉浏览器允许哪些源访问。
2. **前端代理（⭐ 课程采用）**：在 `vue.config.js` 的 `devServer` 里配置 `proxy`，由前端开发服务器把 `/api` 开头的请求转发给 `http://localhost:8080`。课程 PPT 的完整配置（含 `pathRewrite`）：

```js
const { defineConfig } = require('@vue/cli-service')
module.exports = defineConfig({
  transpileDependencies: true,
  devServer: {
    port: 7070,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 转发目标：后端服务
        pathRewrite: {
          '^/api': ''                     // 转发前把路径中的 /api 前缀去掉
        }
      }
    }
  }
})
```

**转发过程拆解**（前端 axios 请求 `/api/admin/employee/login` 为例）：

1. 浏览器发出 `http://localhost:7070/api/admin/employee/login` —— 对浏览器来说这是发给前端自己的，同源，不拦截；
2. 前端开发服务器发现路径以 `/api` 开头，按 `proxy` 规则转发到 `target`；
3. `pathRewrite` 把 `^/api`（行首的 `/api`）替换为空串，实际发给后端的是 `http://localhost:8080/admin/employee/login`；
4. 后端响应原路返回给前端，浏览器全程以为这是同源响应。

上线部署后没有 devServer，由 **Nginx** 做同样的转发。

**别和"端口撞车"搞混：**

| 问题 | 表现 | 原因 |
|---|---|---|
| 端口冲突（第 5 节） | 服务起不来，报 `EADDRINUSE` | 两个程序抢同一个端口 |
| 跨域（本节） | 两个服务都正常，请求被浏览器拦截报 `ERR_FAILED` | 端口不同 = 不同源，浏览器安全策略限制 |

---

## 八、GitHub 代码与学习笔记索引

### 已拉取仓库

**[SihangXie/OUC-Take-Out](https://github.com/SihangXie/OUC-Take-Out)**：基于黑马《瑞吉外卖》的社区学习项目，包含完整源码和一篇很详细的开发笔记。

- 本地目录：`05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/`
- [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/README.md|完整项目学习笔记]]
- 当前本地版本：`028e571`（2022-11-20）
- 注意：这是**社区修改版，不是黑马官方仓库**；项目改名为"听海餐厅"，并把部分登录逻辑改成了邮箱登录。

### 前端代码从哪里看

| 想学什么 | 本地代码 | 看什么 |
|---|---|---|
| HTML + CSS | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/page/login/login.html\|登录页]] · [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/styles/login.css\|登录页样式]] | 页面结构、类名与样式的对应关系 |
| JavaScript | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/page/login/login.html\|登录逻辑]] | `data`、方法、校验、事件与 `async/await` |
| axios | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/js/request.js\|请求封装]] | axios 实例、公共配置、请求/响应拦截器和错误处理 |
| API 分层 | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/api/login.js\|登录 API]] | 页面不直接拼请求，而是调用 API 函数 |
| Vue 基础 | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/page/category/list.html\|分类管理]] | `v-model`、`v-if`、方法、状态与列表渲染 |
| Element UI | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/page/order/list.html\|订单列表]] | `el-table`、`el-input`、`el-button`、日期选择器和弹窗 |
| 完整业务链 | [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/page/food/list.html\|菜品列表]] · [[05 Learning/黑马外卖 · 前端/参考仓库/OUC-Take-Out/src/main/resources/static/backend/api/food.js\|菜品 API]] | 页面 → Vue 方法 → API 函数 → axios → 后端 |

### 推荐阅读顺序

1. **登录页**：先分清 HTML、CSS、Element UI 和 Vue 分别在哪里。
2. **`login.js` + `request.js`**：跟一遍登录请求怎样到达后端、响应怎样回到页面。
3. **订单列表**：观察 axios 返回数据后，Vue 如何交给 Element UI 表格显示。
4. **菜品列表**：自己画出"点击查询 → API 请求 → 更新 `tableData` → 页面重绘"的调用链。

### 版本说明

这套代码使用的是**直接通过 `<script>` 引入的 Vue 2 + Element UI**，和瑞吉外卖老课程的技术形态一致。它适合理解原理和前后端联调，但不等于当前常见的 Vue 3 + Vite + Element Plus 工程结构。

---

## 九、延伸参考（外部权威资料）

> 遇到不懂的概念，优先查这些资料，比搜索引擎更可靠。

### 按主题查

| 主题 | 首推资料 |
|---|---|
| HTML / CSS / JS 基础 | [MDN Web 文档（中文）](https://developer.mozilla.org/zh-CN/docs/Web)——前端领域的"新华字典" |
| JavaScript 系统学习 | [现代 JavaScript 教程](https://zh.javascript.info/)——从入门到进阶，免费且持续更新 |
| Vue 3 | [Vue 3 官方文档（中文）](https://cn.vuejs.org/) |
| Vue 2（课程配套） | [Vue 2 官方文档（中文）](https://v2.cn.vuejs.org/)——本课程用的版本 |
| Element UI（Vue 2） | [Element UI 官方组件文档](https://element.eleme.io/)——用组件前直接查属性表 |
| Element Plus（Vue 3） | [Element Plus 官方文档](https://element-plus.org/zh-CN/) |
| axios / HTTP | [axios 官方文档（中文）](https://axios-http.com/zh/docs/intro)；HTTP 概念查 [MDN HTTP 指南](https://developer.mozilla.org/zh-CN/docs/Web/HTTP) |

### 面试向补充

- [前端面试题汇总（木易杨，GitHub 高分项目）](https://github.com/Advanced-Frontend/Daily-Interview-Question)——每天一道大厂面试题
- [996.ICU 前端面试题库整理](https://github.com/qiu-deqing/FE-interview)——按知识点分类的前端八股

### 工具

- [Vue DevTools](https://devtools.vuejs.org/)——浏览器插件，调试 Vue 状态的必备工具（课程进阶会用到）
